#include<stdio.h>;

void sort(int array [], int value){
    int cp[sizeof(array)];
    for(int x = 0; x<sizeof(array) /sizeof(array[0]); x++){
        
        for(int y = 0; y<sizefo(array) / sizeof(array[0]); x++){
            
        }
    }  

};
int main(){
    int x = {5,3,23,5,43,12,17,16,20};
    
    return 0;
}