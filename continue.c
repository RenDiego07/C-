#include <stdio.h>
#include <string.h>
#define myAge  20
#define user "diego";
int main (){

   
    /*printf("\b fff\b ff");
    for(x; x<20; x++){
        if(x %2 == 0){
            continue;            
        }
        printf("%d\n", x);
    }
    char u = 0;
    char y = 0;
    char valor = getchar();
    printf("%c", valor);
    putchar(valor);E
    //printf("%d", u+y + myAge);
    */

/*int c, i, nwhite, nother;
 int ndigit[10];
 nwhite = nother = 0;
 for (i = 0; i < 10; ++i)
 ndigit[i] = 0;
 while ((c = getchar()) != EOF)
 if (c >= '0' && c <= '9')
    ++ndigit[c-'0'];
else if (c == ' ' || c == '\n' || c == '\t')
 ++nwhite;
 else
 ++nother;
 printf("digits =");
 for (i = 0; i < 10; ++i)
 printf(" %d", ndigit[i]);
 printf(", white space = %d, other = %d\n",
 nwhite, nother);
 */
char name [] = "fiego";

int x;
x = getchar();
printf("%d\n", x);
int y = '0';
printf("%d", y);
 return 0;
}